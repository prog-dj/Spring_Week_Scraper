requests>=2.32,<3
beautifulsoup4>=4.12,<5
# Pinned exactly (not a range) because the Docker image bundles browsers matched to
# this precise version -- see the Dockerfile comment and DEPLOY.md. Bump both
# together if you ever upgrade Playwright.
playwright==1.62.0
Flask>=3.0,<4
Authlib>=1.3,<2
gunicorn>=22.0,<23
python-dotenv>=1.0,<2
