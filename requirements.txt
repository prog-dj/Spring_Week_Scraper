requests>=2.32,<3
beautifulsoup4>=4.12,<5
# Pinned exactly (not a range) because the GitHub Actions workflow installs browsers
# matched to this precise version -- see .github/workflows/scrape.yml. Bump both
# together if you ever upgrade Playwright.
playwright==1.62.0
python-dotenv>=1.0,<2
